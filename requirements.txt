# Pure-Python TrackMate port — runtime dependencies
#
# These are the only packages needed by run_trackmate_python_headless.py.
# No JVM, no Fiji, no PyImageJ.
#
# Tested versions are pinned with >= so newer wheels still resolve.

numpy>=1.23
scipy>=1.10
pandas>=2.0
pillow>=9.0
matplotlib>=3.6
numba>=0.57
